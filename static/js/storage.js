const arrived = document.getElementsByClassName('arrived-btn');


arrived.AddEventListener("click", );


function AddButtonsLocalStorage() {
    const btn = document.querySelector('button');
    const 
}
